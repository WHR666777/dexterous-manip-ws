# NERO 7DoF JS 调试实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 增加法兰目标 IK 与可诊断、拒绝异常命令的 Tracker JS 遥操。

**Architecture:** 独立 NumPy/SciPy 七关节 IK；复用现有 pose_debug 映射。控制器计算与硬件读取分离，只有通过检查的本周期命令可进入 move_js。

**Tech Stack:** Python 3.10、NumPy、SciPy、unittest；已有 NeroArm 和 Tracker 包。

**Spec:** 用户附件 baaa4820-4efa-4a09-b5be-a6b0d5f82641/pasted-text.txt，以及用户批准的“反馈缺失只降级计算，不发送”。

## Global Constraints

- 只新增文件，保留旧遥操及用户工作树；不运行硬件命令、不安装依赖、不提交用户更改。
- T_base_flange；数组 shape=(7,)，运行 rad；CSV 关节字段统一 deg。
- FK <0.002m / <2deg；IK branch 10deg；每轴命令步长5deg；10Hz；正常打印0.5秒。
- 用指定 agilex_teleop/pyAgxArm/asserts/agx_arm_urdf/nero/urdf/nero_description.urdf，相对项目路径。
- 故障不自动复位/回零/失能/重新使能。跳过命令不保证停止或防坠落。

## Task 1: 独立 IK

Files: robot_control/nero_ik.py, tests/test_nero_ik_debug.py。

Interfaces: NeroIK(urdf_path), forward(q), solve(target_pose, q_seed), last_diagnostics。

- [x] 先写并运行 unittest：已知零位 FK、七轴非零自洽、微小目标、非法矩阵、NaN、限位、远目标与周期角。
- [x] XML 按 parent/child 验证 joint1..7，解析原点/轴/限位；使用 `least_squares` 有界多初值，残差包含 seed 正则项，按连续性成本选择通过 FK/branch 验证的收敛解。
- [x] 独立 CLI 自检：`python -m robot_control.nero_ik`，不创建 NeroArm。

## Task 2: 发送门控与运行器

Files: teleop_nero_tracker_move_js_debug.py, tests/test_nero_ik_debug.py。

Interfaces: TeleopSession.step(tracker_pose, feedback, send, reanchor=False), read_feedback(arm), CSVLog。

- [x] 先写失败测试：正常只调用 send 一次；失联只求解不发送；Tracker/target jump 锁定；机器人故障锁定；SDK 异常不推进基准；限幅后重新检查；CSV 包含七轴。
- [x] 复用 FlangeTargetGate、tracker_target 与 read_tracker_pose；新会话维护上一合法 IK 和上一成功调用目标，各自用途分离。
- [x] 实际发送前再次读取状态及新鲜关节反馈；CSV 每周期记录，异常立即打印；R 仅重新对齐，不清除机器人故障。
- [x] 真机入口需人工 MOVE 确认，仅启动时 enable；退出只断开并明确不能保证停止/保持。操作者用硬件急停处理紧急情况。

## Task 3: 验证与交付

Files: docs/nero_js_debug.md。

- [x] 运行 `python -m unittest discover -s tests -p test_nero_ik_debug.py -v`：30项通过。
- [x] 运行 IK CLI 自检、debug --ik-test、--help、py_compile；全部不连接 CAN/SteamVR。
- [x] 文档记录实际 API/URDF顺序/限位、旧代码的已证实问题与未证实假设、参数、日志字段、启动与退出安全边界。

附注：旧诊断测试因原有 `teleop_nero_tracker` 缺失而导入失败；保留旧文件，不把新增测试通过报告为全项目通过。
